using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//public class RemoveEnemyAbility : Ability
{
    //ublic float killRadius = 5f;  // Radius in which enemies will be killed

    //public override void Use(GameObject player)
    {
        // Find all enemies within the radius around the player
        //Collider2D[] enemies = Physics2D.OverlapCircleAll(player.transform.position, killRadius);

        //foreach (Collider2D enemy in enemies)
        {
           // if (enemy.CompareTag("Enemy"))
            {
               // Destroy(enemy.gameObject);  // Destroy the enemy
                //Debug.Log("Enemy Removed!");
            }
        }
    }
}